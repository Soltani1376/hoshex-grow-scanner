name: Validate app

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Check JavaScript syntax
        run: |
          node --check app.js
          node --check v4-core.js
          node --check v4-tasks.js
          node --check v4-ui.js
      - name: Check interface bindings
        run: |
          python3 - <<'PY'
          import re, pathlib, sys
          html = pathlib.Path('index.html').read_text(encoding='utf-8')
          js = pathlib.Path('v4-ui.js').read_text(encoding='utf-8')
          ids = set(re.findall(r'id="([^"]+)"', html))
          refs = set(re.findall(r"\$\('([^']+)'\)", js))
          missing = sorted(refs - ids)
          if missing:
              print('Missing DOM ids:', ', '.join(missing))
              sys.exit(1)
          for path in ['v4-core.js','v4-tasks.js','v4-ui.js']:
              if path not in pathlib.Path('app.js').read_text(encoding='utf-8'):
                  print('Loader does not reference', path)
                  sys.exit(1)
          print('DOM bindings and loader references look good.')
          PY
